/** @type {import('prettier').Config} */
const config = {
  plugins: ['prettier-plugin-tailwindcss'],
  singleQuote: true,
  semi: true,
  tabWidth: 2,
  trailingComma: 'es5',
};

export default config;
